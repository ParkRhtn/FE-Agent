# 복사해서 .env.local 로 사용: cp .env.example .env.local
# 브라우저에는 노출되지 않는 서버 전용 값 (BFF 프록시와 서버 컴포넌트에서 사용)
BACKEND_URL=http://localhost:8000
# 'API로 호출' 예시에 보여 줄 백엔드 주소 (외부 서비스가 직접 부르는 주소). 배포하면 실제 도메인으로
NEXT_PUBLIC_EXTERNAL_API_URL=http://localhost:8000
